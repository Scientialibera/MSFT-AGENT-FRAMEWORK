# ============================================================================
# Agent Creation Environment - DEPRECATED
# ============================================================================
# This file is kept for reference only. 
# ALL CONFIGURATION IS NOW CONSOLIDATED IN: ../env.example
#
# The main .env file in foundry_mcp/ now contains:
# - Agent configuration (FOUNDRY_AGENT_NAME, FOUNDRY_SYSTEM_PROMPT_FILE)
# - Azure OpenAI settings (AZURE_OPENAI_URL, API_KEY, MODEL_DEPLOYMENT_NAME)
# - Azure AI Foundry settings (AZURE_AI_FOUNDRY_URL)
# - Fabric connection settings (FOUNDRY_FABRIC_RESOURCE_ID)
# - All other configuration
#
# If you need to reference the configuration structure, see:
#   - ../env.example (template for all config)
#   - ../env (actual running config)
#
# For standalone use of fabric_agent.py outside the MCP server context,
# you can still set these environment variables locally, but use the same
# variable names as defined in ../env.example
#
# This file will be removed in a future version.
# ============================================================================

